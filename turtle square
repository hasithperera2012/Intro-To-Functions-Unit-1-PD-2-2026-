# String for characters
name = "Inaya"
# input asks the user aquestion and records the answer
# what we write in input argument is whatuser sees
# input ALWAYS outputs a string
bill = input("Howmuch was the bill")
print(bill)

if bill == 10
    print("Match")

else:
    print("no match")

# inter for whoe number
# amt=100
# Float uses decimal
# amt_two=99.99

# Boolean
x = True
y = False